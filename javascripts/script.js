// Generated by CoffeeScript 1.4.0

$(function() {
  return $('#maximage').maximage()({
    cycleOptions: {
      fx: "fade",
      speed: 1000,
      timeout: 0,
      prev: "#arrow_left",
      next: "#arrow_right",
      pause: 1
    },
    onFirstImageLoaded: function() {
      jQuery("#cycle-loader").hide();
      return jQuery("#maximage").fadeIn("fast");
    },
    cssBackgroundSize: false,
    backgroundSize: function($item) {
      if ($item.data("h") > $item.data("w")) {
        if ($.Window.data("w") / $.Window.data("h") < $item.data("ar")) {
          return $item.height(($.Window.data("w") / $item.data("ar")).toFixed(0)).width($.Window.data("w"));
        } else {
          return $item.height($.Window.data("h")).width(($.Window.data("h") * $item.data("ar")).toFixed(0));
        }
      } else {
        if ($.Window.data("w") / $.Window.data("h") < $item.data("ar")) {
          return $item.height($.Window.data("h")).width(($.Window.data("h") * $item.data("ar")).toFixed(0));
        } else {
          return $item.height(($.Window.data("w") / $item.data("ar")).toFixed(0)).width($.Window.data("w"));
        }
      }
    }
  });
});
